#include <string>

struct messages {
    static inline const std::string NOT_ENOUGH_MEMORY = "Not enough memory";
    static inline const std::string BAD_ARGUMENT = "Bad argument";
    static inline const std::string ALREADY_FREE = "Memory is already free";
};